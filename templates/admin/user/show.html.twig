{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
    <div class="container">
    <h1>Utilisateur</h1>

    <a class="btn btn-sm btn-outline-light my-4" href="{{ path('user_index') }}">Retour à la liste</a>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Droits</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>



    {{ include('/admin/user/_delete_form.html.twig') }}

    </div>
{% endblock %}
