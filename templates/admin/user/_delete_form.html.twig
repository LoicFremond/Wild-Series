<form method="post" action="{{ path('user_delete', {'id': user.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer {{ user.username}} ayant les droits {{ user.slug}} ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('Delete' ~ user.id) }}">
    <button class="btn btn-sm btn-outline-light my-3">Supprimer</button>
</form>
