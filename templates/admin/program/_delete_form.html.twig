<form method="post" action="{{ path('program_delete', {'id': program.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer {{ program.title}} ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ program.id) }}">
    <button class="btn btn-sm btn-outline-light my-3">Supprimer</button>
</form>
