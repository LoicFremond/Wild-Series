{% extends 'base.html.twig' %}

{% block title %}Modifier une catégorie | Admin{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-5 text-center">Editer</h1>
        <a class="btn btn-sm btn-outline-light my-4" href="{{ path('category_index') }}">{{ 'Back'|trans }}</a>

        <div class="row">
            <div class="col col-md-4">
                {{ include('admin/category/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>
        {{ include('admin/category/_delete_form.html.twig') }}
    </div>
{% endblock %}
