{% extends 'base.html.twig' %}

{% block title %}Catégorie {{ category.name }} | Admin{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mb-5">Visualiser</h1>

        <a class="btn btn-sm btn-outline-light my-3" href="{{ path('category_index') }}">{{ 'Back'|trans }}</a>

        <table class="table table-dark">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
            </tbody>
        </table>


        <a class="btn btn-sm btn-outline-light my-3s" href="{{ path('category_edit', {'id': category.id}) }}">Editer</a>

        {{ include('admin/category/_delete_form.html.twig') }}
    </div>
{% endblock %}
