{% extends 'base.html.twig' %}

{% block title %}Modification Acteur | Admin{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-5 text-center">Modification Acteur</h1>
        <a class="btn btn-sm btn-outline-light my-4" href="{{ path('actor_index') }}">{{ 'Back'|trans }}</a>

        <div class="row">
            <div class="col-md-4 image-container mb-3">
                <img id="img" src="" alt="image non disponible">
            </div>
            <div class="col-md-8">
                {{ include('admin/actor/_form.html.twig', {'button_label': 'Update'}) }}
                {{ include('admin/actor/_delete_form.html.twig') }}
            </div>
        </div>

    </div>
{% endblock %}
