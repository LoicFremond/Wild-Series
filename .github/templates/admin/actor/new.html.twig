{% extends 'base.html.twig' %}

{% block title %}Nouvel acteur | Admin{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-5 text-center">Nouvel acteur</h1>
        <a class="btn btn-sm btn-outline-light" href="{{ path('actor_index') }}">{{ 'Back'|trans }}</a>
        <div class="row">
            <div class="col-md-4 image-container mb-3">
                <img id="img" src="" alt="image non disponible">
            </div>
            <div class="col-md-8">
                {{ include('admin/actor/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
