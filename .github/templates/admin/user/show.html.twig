{% extends 'base.html.twig' %}

{% block title %}{{ 'User'|trans }}{% endblock %}

{% block body %}
    <div class="container">
    <h1 class="my-5 text-center">{{ 'User'|trans }}</h1>

    <a class="btn btn-sm btn-outline-light my-4" href="{{ path('user_index') }}">{{ 'Back'|trans }}</a>
        <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Droits</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>



    {{ include('/admin/user/_delete_form.html.twig') }}

    </div>
{% endblock %}
